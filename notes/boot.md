* connection status and watcher
* get theme
* get language
* auth: check token in storage
* auth: assign to redux store
* auth: if ! connected, send to authscreen
* auth: if connected, connect socket and init app
